class class_name {

  //constructor always '__constrcut' two underscore
  public function __construct([parameterList]) {
    //statements to execute
    //ex, open the file in constructor
  }

  //garbage collection : collect all garbage and automatically ```  php, they do garbage collection but allow to have
  //destruct method
  public function __destruct() {
    //statements to execute
    //execute 
    //close the file , invoked
  }

  [public | private | protected ]
    function functionName ([parameterList]) {
      //statements to execute
    }
}